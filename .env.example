# Gemini API settings
# Copy this file to .env.local and replace with your real key.
GEMINI_API_KEY=replace_with_your_new_key

# Optional: Override model if needed.
# Recommended default is gemini-2.0-flash.
GEMINI_MODEL=gemini-2.0-flash
